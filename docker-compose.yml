version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: chessi
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass